---
version: '1.0'

project:
  name: "%(slug)s"
  slug: "%(slug)s"

components:
  # --- Node.js JSON translations ---
  - name: Locales JSON
    slug: locales-json
    filemask: locales/{lang}/*.json
    source_language: en
    language_map:
      cy: cy
    file_format: json
    vcs: git
    repo: git@github.com:companieshouse/%(slug)s.git
    push: git@github.com:companieshouse/%(slug)s.git
    branch: main
    # Automatically add new languages when new files appear
    add_new_translations: true
    # Built-in Weblate checks
    checks:
      - consistency
      - placeholders
      - punctuation
      - capitalization
